/**
 * Cờ ẩn tạm các trường ở màn hình soạn quiz LMS. Chỉ ẩn UI — schema, API và
 * logic chấm/feedback vẫn nguyên; đổi sang `true` là hiện lại.
 */
/** Độ khó (1–5) của quiz. */
export const SHOW_QUIZ_DIFFICULTY = false;
/** Bật/tắt "Đánh giá độ tự tin" (confidence) sau mỗi câu trả lời. */
export const SHOW_QUIZ_CONFIDENCE = false;
